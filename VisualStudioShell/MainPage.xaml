<Page
    x:Class="VisualStudioShell.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VisualStudioShell"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:controls="using:VisualStudioShell.Controls"
    mc:Ignorable="d" d:DesignWidth="600">
  <Page.Resources>
    <SolidColorBrush x:Key="VsMainBackground" Color="#2D2D30"/>
  </Page.Resources>

  <Grid Background="{StaticResource VsMainBackground}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition/>
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal">
      <StackPanel.Resources>
        <Style TargetType="Button">

        </Style>
      </StackPanel.Resources>
      <Button Content="File" VerticalAlignment="Bottom">
        <Button.Flyout>
          <MenuFlyout>
            <MenuFlyoutSubItem Text="New">
              <controls:MenuFlyoutImageItem Text="Project"/>
              <controls:MenuFlyoutImageItem Text="Website"/>
            </MenuFlyoutSubItem>
            <MenuFlyoutSeparator/>
            <controls:MenuFlyoutImageItem Text="Close Solution"/>
            <MenuFlyoutSeparator/>
            <controls:MenuFlyoutImageItem Text="Save" Icon="Images/Thomas_farbig_quadrat.jpg"/>
            <controls:MenuFlyoutImageItem Text="Save All"/>
            <MenuFlyoutSeparator/>
            <controls:MenuFlyoutImageItem Text="Exit"/>
          </MenuFlyout>
        </Button.Flyout>
      </Button>
      <Button Content="Edit"/>
      <Button Content="View"/>
      <Button Content="Project"/>
      <Button Content="Build"/>
      <Button Content="Debug"/>
      <Button Content="Team"/>
      <Button Content="Tools">
        <Button.Flyout>
          <MenuFlyout>
            <MenuFlyoutItem Text="Extensions and Updates" IsEnabled="False" />
            <MenuFlyoutItem Text="Connect to Database..." IsEnabled="False" />
            <MenuFlyoutSeparator/>
            <MenuFlyoutItem Text="Options..." />
          </MenuFlyout>
        </Button.Flyout>
      </Button>
      <Button Content="Test"/>
      <Button Content="Analyze"/>
      <Button Content="Window"/>
      <Button Content="Help"/>
    </StackPanel>
  </Grid>
</Page>